## 智能体技能

### Issue 跟踪器

本仓库的 Issue、规格说明与 ticket 以 Markdown 文件形式存放在 `.scratch/<feature-slug>/` 下。详见 `docs/agents/issue-tracker.md`。

### 分流标签

五种规范的分流角色，每个标签字符串与其角色名相同。详见 `docs/agents/triage-labels.md`。

### 领域文档

单上下文：仓库根目录下的一份 `CONTEXT.md` 加上 `docs/adr/`。详见 `docs/agents/domain.md`。
